# Environment
NODE_ENV=development
LOG_LEVEL=info

# Harbor Environment (local, staging, production)
# - local: Uses in-memory PostgreSQL (no external database needed)
# - staging/production: Uses real PostgreSQL (requires DATABASE_URL)
HARBOR_ENV=local

# Database Configuration
# For local development: Leave empty (in-memory database will be used)
# For staging/production: Set to your Cloud SQL connection string
DATABASE_URL=

# Service-specific databases (optional - for full isolation in staging/production)
# DATABASE_URL_TENDERING=postgresql://user:password@localhost:5432/harbor_tendering
# DATABASE_URL_USER=postgresql://user:password@localhost:5432/harbor_user
# DATABASE_URL_WALLET=postgresql://user:password@localhost:5432/harbor_wallet
# DATABASE_URL_SETTLEMENT=postgresql://user:password@localhost:5432/harbor_settlement

# Database Migration
# Auto-run migrations on startup (default: true for local, false for staging/production)
# Set to 'false' to disable auto-migration in local
# Set to 'true' to enable auto-migration in staging/production (not recommended)
# DB_AUTO_MIGRATE=true

# Circle API (for wallet management)
CIRCLE_API_KEY=your_circle_api_key
CIRCLE_ENTITY_SECRET=your_entity_secret
CIRCLE_WALLET_SET_ID=default
CIRCLE_USDC_TOKEN_ID=USDC

# Stripe API (for fiat onramp/offramp)
STRIPE_API_KEY=your_stripe_api_key

# Platform Fees (percentage as decimal, e.g., 0.025 = 2.5%)
FEE_BUYER_PERCENTAGE=0.025
FEE_SELLER_PERCENTAGE=0.025

# Platform Wallets (wallet IDs for platform-controlled omnibus accounts)
ESCROW_WALLET_ID=  # Wallet ID for holding escrow funds during IN_PROGRESS
REVENUE_WALLET_ID=  # Wallet ID for collecting platform fees

# Service ports (defaults defined in libs/config/src/ports.ts)
# PORT=3000  # Override specific service port if needed
